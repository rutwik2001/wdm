// HeroSection.js
import React from 'react';
import './HeroSection.css'; // Make sure to create a corresponding CSS file

const HeroSection = () => {
    return (
        <div className="hero-section">
            <h1>Transformative Approach to Healthcare</h1>
            <p>Our skilled doctors have tremendous experience with a wide range of ailments to serve the needs of our patients.</p>
            <button>Make an appointment</button>
        </div>
    );
};

export default HeroSection;
